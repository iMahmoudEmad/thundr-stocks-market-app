/**
 * This file exports a QUERY_KEYS object that defines a set of query keys used for managing data queries with react-query.
 * Query keys are integral to caching, fetching, and invalidating data in an efficient manner.
 */

export const QUERY_KEYS = {
  Dividends: ["DIVIDENDS"],
};
